apiVersion: v2
name: sluice
description: L4/L7 Proxy & API Gateway with Service Discovery
type: application
version: 0.1.1
# Must be a tag that exists in ghcr.io/abd-ulbasit/sluice: with image.tag
# unset, _helpers.tpl resolves the image to <repository>:<appVersion>. The CI
# docker job publishes {{version}} for every pushed v* tag, so this tracks the
# release tag.
appVersion: "1.0.1"
keywords:
  - proxy
  - gateway
  - load-balancer
  - api-gateway
  - tcp-proxy
maintainers:
  - name: Abdul Basit
    email: basit@basit.engineer
home: https://github.com/abd-ulbasit/sluice
sources:
  - https://github.com/abd-ulbasit/sluice
annotations:
  category: Infrastructure
