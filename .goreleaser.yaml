# GoReleaser configuration
# https://goreleaser.com

version: 1

# Set explicitly. Left unset, GoReleaser infers it, and the archive
# name_template below interpolates whatever it inferred — which is how v1.0.0
# shipped as gogate_1.0.0_linux_amd64.tar.gz.
project_name: sluice

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - id: sluice
    main: ./cmd/sluice
    binary: sluice
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.buildTime={{.Date}}

archives:
  - id: sluice
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    files:
      - LICENSE
      - README.md
      - config.example.yaml

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - Merge pull request
      - Merge branch

# Note: Docker images are built via the separate Docker job in CI workflow
# This allows us to use our multi-stage Dockerfile with distroless runtime

release:
  draft: false
  prerelease: auto
